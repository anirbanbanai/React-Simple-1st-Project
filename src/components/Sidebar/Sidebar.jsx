import React from 'react';

const Sidebar = (props) => {
    console.log(props.bokkmark);
    let my = props.bokkmark;
    return (
        <div >
            
        </div>
    );
};

export default Sidebar;